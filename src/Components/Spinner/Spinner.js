import { Spinner as SpinnerBst } from "react-bootstrap";

const Spinner = () => {
  return (
    <SpinnerBst animation="grow" variant="primary" className="mt-3 mb-3" />
  );
};

export default Spinner;
